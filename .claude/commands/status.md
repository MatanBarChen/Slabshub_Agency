---
description: תמונת מצב מהירה של הסוכנות — לוז, אישורים ממתינים, חסמים
argument-hint: [ריק]
---

Read `drafts/state.json` (the single source of truth — do not re-scan draft files) and present Matan a quick Hebrew status: the weekly schedule, pending approvals, and blockers. Per the visual reporting rule, attach the schedule widget (colored row-per-slot: green=approved, yellow=pending, red=blocked). If Matan asks a follow-up that state.json can't answer, only then open the specific draft file.
